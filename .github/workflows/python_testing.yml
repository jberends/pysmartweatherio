name: Test pysmartweatherio

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 6
      fail-fast: False
      matrix:
        python-version: [3.6, 3.7, 3.8, 3.9, pypy3]

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v1
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install basic dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -U setuptools>=38.6.0 wheel>=0.31.0
        pip install -r requirements.txt

    - name: Perform Testing
      run: |
        pip install pytest pytest-runner
        pytest tests

# TODO: you may want to enable this check if linting in the project is fixed
# TODO: run flake8 locally and ccrrect all code. Autoflow in pycharm or black
# TODO: may help in resolving many pains.
#    - name: Check docs and distribution
#      if: matrix.python-version == 3.8
#      run: |
#        pip install flake8 pydocstyle readme_renderer[md] twine>=2.0.0
#
#        flake8 pysmartweatherio --count --show-source --statistics
#        pydocstyle pysmartweatherio
#        python setup.py bdist_wheel --universal
#        twine check dist/*
